name: Main CI
on:
  push:

jobs:
  get-vars:
    uses: ./.github/workflows/nodevars.yml

  main:
    needs: get-vars
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ needs.get-vars.outputs.node-version }}
          cache: npm
      - name: Setup npm
        run: npm i -g npm@${{ needs.get-vars.outputs.npm-version }}
      - name: Cache node modules
        id: node_modules_cache
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ env.cache-name }}-${{ hashFiles('package-lock.json') }}
      - name: Install
        if: steps.node_modules_cache.outputs.cache-hit != 'true'
        run: npm ci
      - name: Type check
        run: npm run typecheck
      - name: Lint check
        run: npm run lint
      - name: Test
        run: npm run test
  windows-test:
    needs: get-vars
    runs-on: windows-latest
    env:
      TEST_IGNORE_256_ICON: 1
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ needs.get-vars.outputs.node-version }}
          cache: npm
      - name: Cache node modules
        id: node_modules_cache
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ env.cache-name }}-${{ hashFiles('package-lock.json') }}
      - name: Install
        if: steps.node_modules_cache.outputs.cache-hit != 'true'
        # use npx instead of installing npm globally to avoid permission error
        run: npx -y npm@${{ needs.get-vars.outputs.npm-version }} ci
      - name: Test (x86)
        run: npx -y npm@${{ needs.get-vars.outputs.npm-version }} run test:win-x86
      - name: Test (x64)
        run: npx -y npm@${{ needs.get-vars.outputs.npm-version }} run test:win-x64
